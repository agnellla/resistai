"""
dashboard/app.py  (PLACEHOLDER - do not build yet)
-------------------------------------------------
Planned Streamlit demo for the video:

  - upload an image
  - pick a corruption (jpeg / blur / noise / ...) with a slider for strength
  - show the model's prediction + confidence on clean vs corrupted
  - side-by-side: baseline model vs transformation-aware model

Run later with:  streamlit run dashboard/app.py

Left as a stub so `streamlit run` shows a friendly "not built yet" page.
"""

import streamlit as st

st.title("ResistAI - demo (not built yet)")
st.write(
    "Placeholder. Build this after the baseline and transformation-aware "
    "models are trained and evaluated."
)
